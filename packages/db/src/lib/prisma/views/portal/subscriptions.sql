SELECT
  subscriptions.id,
  subscriptions.object,
  subscriptions.cancel_at_period_end,
  subscriptions.current_period_end,
  subscriptions.current_period_start,
  subscriptions.default_payment_method,
  subscriptions.items,
  subscriptions.metadata,
  subscriptions.pending_setup_intent,
  subscriptions.pending_update,
  subscriptions.status,
  subscriptions.application_fee_percent,
  subscriptions.billing_cycle_anchor,
  subscriptions.billing_thresholds,
  subscriptions.cancel_at,
  subscriptions.canceled_at,
  subscriptions.collection_method,
  subscriptions.created,
  subscriptions.days_until_due,
  subscriptions.default_source,
  subscriptions.default_tax_rates,
  subscriptions.discount,
  subscriptions.ended_at,
  subscriptions.livemode,
  subscriptions.next_pending_invoice_item_invoice,
  subscriptions.pause_collection,
  subscriptions.pending_invoice_item_interval,
  subscriptions.start_date,
  subscriptions.transfer_data,
  subscriptions.trial_end,
  subscriptions.trial_start,
  subscriptions.schedule,
  subscriptions.customer,
  subscriptions.latest_invoice,
  subscriptions.plan,
  subscriptions.updated_at
FROM
  stripe.subscriptions;